<li class="document">
  <h3><%= link_to document.title, document.url %></h3>
  <dl>
    <% document.metadata.each do |entry| %>
      <dt><%= entry[:name] %></dt>
      <dd><%= document_metadata_value(entry[:value], entry[:type]) %>
    <% end %>
  </dl>
</li>
