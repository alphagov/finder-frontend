<% content_for :title, title %>
<% content_for :head do %>
  <% unless params[:page] %>
    <meta name="robots" content="noindex">
  <% end %>
<% end %>

<%
  formatted_description = nil
  if @checklist_questions.current_question.description
    formatted_description = render("govuk_publishing_components/components/govspeak", {
      content: @checklist_questions.current_question.description.html_safe
    })
  end
%>

<div class="govuk-width-container">
  <%= render 'govuk_publishing_components/components/breadcrumbs', breadcrumbs: breadcrumbs %>

  <div class="govuk-grid-row">
    <div class="govuk-grid-column-two-thirds">
      <form action="<%= find_brexit_guidance_path %>" method="get" id="finder-qa-facet-filter-selection" data-module="track-qa-choices">
        <% if @checklist_questions.current_question.multiple? %>
          <div class="govuk-!-margin-top-8">
            <%= render 'question_multiple_choice', {
              current_question: @checklist_questions.current_question,
              formatted_description: formatted_description
            } %>
          </div>
        <% elsif @checklist_questions.current_question.single_wrapped? %>
          <div class="govuk-!-margin-top-8">
            <%= render 'question_single_wrapped_choice', {
              current_question: @checklist_questions.current_question,
              formatted_description: formatted_description
            } %>
          </div>
        <% else %>
          <div class="govuk-!-margin-top-8">
            <%= render 'question_single_choice', {
              current_question: @checklist_questions.current_question,
              formatted_description: formatted_description
            } %>
          </div>
        <% end %>
        <%= render 'form_data', current_question: @checklist_questions.current_question %>
        <%= render 'hint_text', current_question: @checklist_questions.current_question %>
        <div class="govuk-form-footer">
          <%= render "govuk_publishing_components/components/button", {
            text: "Next"
          } %>
          <a class="govuk-form-footer__alt-cta-link" href="<%= skip_link_url %>">Skip this question</a>
        </div>
      </form>
    </div>
  </div>
</div>
